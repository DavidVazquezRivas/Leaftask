type: object
properties:
  code:
    type: string
    example: 'session.refresh.expired'
  message:
    type: string
    example: 'An unexpected error occurred. Please try again later.'
required:
  - code
  - message
