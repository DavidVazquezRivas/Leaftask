type: object
properties:
  status:
    type: string
    enum: [accept, reject]
    description: The status to set for the invitation. Use 'accept' to accept the invitation or 'reject' to decline it.
    example: 'accept'
required:
  - status
