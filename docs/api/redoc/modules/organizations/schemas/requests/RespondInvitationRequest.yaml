type: object
properties:
  status:
    type: string
    enum: [accepted, rejected, cancelled]
    description: The status to set for the invitation. Use 'accepted' to accept the invitation or 'rejected' to decline it and 'cancelled' to cancel the invitation.
    example: 'accepted'
required:
  - status
